sudo: required
dist: trusty
services:
  - docker
before_install:
  - sudo apt-get update -yq
  - sudo apt-get -o Dpkg::Options::="--force-confnew" install -yq docker-ce kubectl
  - gcloud version
  - kubectl help
script:
  - echo "Hello world"
  - kubectl get no
notifications:
  email:
    recipients:
      - harsh@portworx.com
    on_success: change
    on_failure: always
